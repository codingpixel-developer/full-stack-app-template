## Global Conventions

- Always read `CLAUDE.md` before starting any work
- Use `.claude/skills/` for guided workflows
- Use `.claude/rules/` for coding standards and conventions — always follow these when writing any code
- **Function size** — no function may exceed **300–350 lines**; split large functions into smaller, focused ones (several small functions are always preferred over one big function)
